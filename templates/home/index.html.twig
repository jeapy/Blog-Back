{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>



<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    {% if is_granted('ROLE_ADMIN') %}
            <div class="mb-3">
            Got To Admin , <a href="{{ path('admin') }}">Adminpage</a>
            </div>
    {% endif %}

{% if not app.user %}
     <div class="mb-3">
      <a href="{{ path('app_login') }}">Login</a>  | 
      <a href="{{ path('app_register') }}">Register</a>
      </div>
 {% else %}
 <div class="mb-3">
      <a href="{{ path('app_logout') }}">Logout</a> 
      </div>
 {% endif %}

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>/Users/macbookair/Projets/App_BackEnd/blog/src/Controller/HomeController.php</code></li>
        <li>Your template at <code>/Users/macbookair/Projets/App_BackEnd/blog/templates/home/index.html.twig</code></li>
    </ul>

    <div class="card mb-4 ">
    This friendly message is coming from:
    {{ render_chart(chart) }}
</div>
</div>
{% endblock %}
